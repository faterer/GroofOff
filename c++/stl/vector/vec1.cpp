/*
 * vec1.cpp
 *
 *  Created on: May 9, 2014
 *      Author: mawang
 */
#include <iostream>
#include <vector>
#include <iterator>

using namespace std;
/*
int main()
{
	vector<int> vec;

	vec.push_back(1);
	vec.push_back(2);
	vec.push_back(3);

	vector<int>::iterator pos1;
	for (pos1 = vec.begin(); pos1 != vec.end(); ++pos1)
		cout << *pos1;
	cout << endl;

	vector<int>::reverse_iterator pos2;
	for (pos2 = vec.rbegin(); pos2 != vec.rend(); ++pos2)
		cout << *pos2;
	cout << endl;
}
*/


