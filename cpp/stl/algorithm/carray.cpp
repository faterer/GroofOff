#include <algorithm>
#include <functional>
#include "carray.hpp"
#include "algostuff.hpp"

using namespace std;

/*
int main()
{
	carray<int, 10> a;

	for(unsigned i = 0; i < a.size(); ++i)
	{
		a[i] = i + 1;
	}

	PRINT_ELEMENTS(a);

	reverse(a.begin(), a.end());
	PRINT_ELEMENTS(a);

	transform(a.begin(), a.end(),
			  a.begin(),
			  negate<int>());

	PRINT_ELEMENTS(a);
}
*/
